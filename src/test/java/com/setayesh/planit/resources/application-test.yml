spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:file:./planit_test_db   
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: false                    
  jpa:
    hibernate:
      ddl-auto: create-drop             

logging:
  level:
    root: WARN
